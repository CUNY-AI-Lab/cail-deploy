interface:
  display_name: "Kale Deploy"
  short_description: "Build Cloudflare apps for Kale Deploy."
  brand_color: "#c2410c"
  default_prompt: "Use $kale-deploy to build a small web app that fits the Kale runtime. Choose the lightest starter shape that fits: static for pure publishing, worker only when request-time behavior is needed."

policy:
  allow_implicit_invocation: true
